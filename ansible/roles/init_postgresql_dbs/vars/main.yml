#SPDX-License-Identifier: MIT-0
---
# vars file for roles/create_postgresql_users
schemes:
  - name: mealie_db
    username: mealie
    password: "{{ mealie_password }}"
  - name: authentik
    username: authentik
    password: "{{ authentik_password }}"
  - name: zabbix
    username: zabbix
    password: "{{ zabbix_password }}"
