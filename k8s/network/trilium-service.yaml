apiVersion: v1
kind: Service
metadata:
  name: trilium-service
spec:
  type: ClusterIP
  selector:
    app: trilium
  ports:
    - protocol: TCP
      port: 80        # Service port inside the cluster
      targetPort: 8080 # Container port